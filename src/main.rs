fn main() {}

struct Board {
    line_1: Vec<u32>,
    line_2: Vec<u32>,
    line_3: Vec<u32>,
    line_4: Vec<u32>,
    line_5: Vec<u32>,
}

impl Board{
    fn from(board_str: String) -> Self{

    }
}
